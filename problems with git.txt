V?n d?:
M?t quy trình git khác du?ng nhu dang ch?y trong kho luu tr? này, ví d?: 
trình ch?nh s?a du?c m? b?i 'git commit'. Vui lòng d?m b?o t?t c? các quy trình 
du?c ch?m d?t sau dó th? l?i. N?u v?n không thành công, m?t quá trình git 
có th? dã b? l?i trong kho luu tr? này tru?c dó: 
xóa t?p theo cách th? công d? ti?p t?c.

Dung d?ch:
M? kho luu tr? c?c b?
Theo m?c d?nh .git b? ?n trong khi kh?i t?o kho luu tr? c?c b?, b? ?n thu m?c .git.
Thu m?c OPen .git
Trong t?p "index.lock" này có s?n, hãy xóa t?p dó


Problem:
Another git process seems to be running in this repository, e.g.
an editor opened by 'git commit'. Please make sure all processes
are terminated then try again. If it still fails, a git process
may have crashed in this repository earlier:
remove the file manually to continue.

Solution:
Open local repository
By default .git is hidden while initializing local repositort, unhide .git folder.
OPen .git folder
Under this "index.lock" file is available, delete that file

